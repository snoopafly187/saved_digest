name: Reddit Digest

# Trigger manually or on a daily schedule at 08:00 UTC
on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'

jobs:
  reddit-digest:
    runs-on: ubuntu-latest

    steps:
      # 1) Checkout your repo
      - name: Checkout code
        uses: actions/checkout@v3

      # 2) Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # 3) Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml praw requests

      # 4) Run your digest script
      - name: Generate Reddit Digest
        env:
          OPENAI_API_KEY:    ${{ secrets.OPENAI_API_KEY }}
          OPENAI_PROJECT_ID: ${{ secrets.OPENAI_PROJECT_ID }}
          REDDIT_CLIENT_ID:  ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_SECRET:     ${{ secrets.REDDIT_SECRET }}
          REDDIT_USER_AGENT: ${{ secrets.REDDIT_USER_AGENT }}
          REDDIT_USERNAME:   ${{ secrets.REDDIT_USERNAME }}
          REDDIT_PASSWORD:   ${{ secrets.REDDIT_PASSWORD }}
        run: |
          python digest.py

      # 5) Show what got generated
      - name: List generated digests
        run: |
          echo "=== contents of digests/ ==="
          ls -R digests || echo "(no digests found)"

      # 6) Upload as an artifact for easy download
      - name: Upload digest artifact
        uses: actions/upload-artifact@v3
        with:
          name: reddit-digest
          path: digests/*.md

      # 7) Commit & push only if there are new/changed files
      - name: Commit & push digest
        shell: bash
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Only if any markdown files exist
          if compgen -G "digests/*.md" > /dev/null; then
            git add digests/*.md

            # Only if there are staged changes
            if ! git diff --cached --quiet; then
              git commit -m "chore: update Reddit digest for $(date +'%Y-%m-%d')"
              git push
            else
              echo "▶️ No changes to digests/*.md, skipping commit"
            fi
          else
            echo "▶️ No digests/*.md files found, skipping commit"
          fi
